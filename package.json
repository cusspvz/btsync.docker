{
  "name": "btsyncctl",
  "version": "1.2.0",
  "description": "btsync control",
  "main": "bin/ctl.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changelog-tag": "sed -i -e \"1s/^## Upcoming/## Upcoming\\n\\n\\n## $npm_package_version/\" CHANGELOG.md",
    "preversion": "npm run changelog-tag; git add CHANGELOG.md;",
    "postversion": "git push origin master; git push origin --tags"
  },
  "author": "Jose Moreira <jose.moreira@findhit.com>",
  "license": "GPL-3.0",
  "dependencies": {
    "debug": "^2.2.0",
    "sync-exec": "^0.6.2",
    "yargs": "^3.25.0"
  }
}
